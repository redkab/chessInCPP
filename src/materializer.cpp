#include<iostream>
#include "texspri.h"

void renderVisualBoard(char board[8][8])
{
    int k=0, l=0;
    for(int i=0; i<8; i++)
    {
        for(int j=0; j<8; j++)
        {
            char c = board[i][j];

            switch (c)
            {
            }
        }
    
    }
}
